import "./globals.css";
import Header from "./components/Header";
import Footer from "./components/Footer";
export const metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({ children }) {
  return (
    <html
    >
      <body className="min-h-full flex flex-col">
        <Header />
        {children}
        <Footer />
      </body>
    </html>
  );
}

///WE HAD CALLED HEADER AND FOOTER COMPONENT IN THE ROOT LAYOUT SO THAT THEY CAN BE SHOWN ON ALL THE PAGES OF THE APPLICATION.
///WHILE WE HAD CALLED THE BANNER AND LATEST PRODUCT COMPONENT IN THE HOME PAGE ONLY SO THAT THEY CAN BE SHOWN ON THE HOME PAGE ONLY.
// AND PAGES CAN BE CHANGED BUT THE LAYOUT REMAINS SAME THROUGHOUT THE APPLICATION.
// IN THE ROOT LAYOUT WE CAN ALSO CALL THE CSS FILES WHICH WE WANT TO USE THROUGHOUT THE APPLICATION.
///WE ALWAYS MADKE ABOUT US AND LOADING COMPONENTS INSIDE APP FOLDER BECAUSE THEY ARE COMMON COMPONENTS AND CAN BE USED THROUGHOUT THE APPLICATION.


///FOR API FETCHING, WE CAN USE AXIOS OR FETCH INSIDE THE COMPONENTS AND THEN PASS THE DATA TO THE COMPONENTS AS PROPS. 
// OR WE CAN ALSO USE THE GET SERVER SIDE PROPS OR GET STATIC PROPS TO FETCH THE DATA AND THEN PASS IT TO THE COMPONENTS AS PROPS.